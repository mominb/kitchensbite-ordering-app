import { View } from "react-native";
import { colors } from "../theme";

const ItemSeperator = () => {
   return (
      <View style={{ width: "100%", backgroundColor: colors.divider, height: 1 }} />
   );
};

export default ItemSeperator;
